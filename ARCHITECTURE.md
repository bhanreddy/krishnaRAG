# GitaRAG - Architecture & System Design

## System Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      USER INTERFACE                          │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            Beautiful Web Frontend                    │  │
│  │  - Question Input                                   │  │
│  │  - Answer Display                                   │  │
│  │  - Source References                               │  │
│  │  - Loading States                                  │  │
│  │  - Modern CSS Animations                           │  │
│  └──────────────┬───────────────────────────────────┬──┘  │
└─────────────────┼───────────────────────────────────┼──────┘
                  │                                   │
              HTTP/JSON              CORS Enabled    │
                  │                                   │
                  ▼                                   ▼
┌──────────────────────────────────────────────────────────────┐
│                    FASTAPI BACKEND                           │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         REST API Endpoints (7 endpoints)            │  │
│  │                                                      │  │
│  │  GET /health              Check service status      │  │
│  │  GET /llm/status          Check LLM connection      │  │
│  │  GET /llm/models          List available models     │  │
│  │  POST /llm/test           Test LLM with prompt      │  │
│  │  POST /build_index        Build FAISS index         │  │
│  │  POST /search             Search passages           │  │
│  │  POST /query              Full RAG pipeline         │  │
│  └──────────────┬────────────────────────┬─────────────┘  │
│                 │                        │                 │
│           ┌─────▼─────┐          ┌──────▼──────┐          │
│           │   RAG     │          │ LLM Client  │          │
│           │  Engine   │          │ Integration │          │
│           └─────┬─────┘          └──────┬──────┘          │
└─────────────────┼──────────────────────┼─────────────────┘
                  │                      │
                  ▼                      ▼
┌──────────────────────────────────────────────────────────────┐
│              CORE PROCESSING ENGINES                         │
│                                                              │
│  ┌──────────────────────┐     ┌───────────────────────┐    │
│  │   Bhagavad Gita      │     │  Local LLM Service    │    │
│  │   RAG Engine         │     │  Integration          │    │
│  │                      │     │                       │    │
│  │ • Corpus Loading     │     │ • Ollama Support      │    │
│  │ • Embedding Model    │     │ • Model Management    │    │
│  │ • FAISS Index        │     │ • Prompt Building     │    │
│  │ • Semantic Search    │     │ • Response Generation │    │
│  │ • Context Retrieval  │     │ • Error Handling      │    │
│  └──────────┬───────────┘     └──────────┬────────────┘    │
└─────────────┼──────────────────────────┼────────────────┘
              │                          │
              ▼                          ▼
┌──────────────────────┐      ┌──────────────────────────┐
│   Sentence          │      │    Ollama/LM Studio     │
│   Transformers      │      │    Local LLM Service    │
│                     │      │                         │
│ • all-MiniLM-L6-v2  │      │ Models Available:       │
│ • Embedding Gen     │      │ - mistral               │
│ • Semantic Sim      │      │ - llama2                │
└──────────┬──────────┘      │ - neural-chat           │
           │                 │ - dolphin-mixtral       │
           ▼                 └──────────┬──────────────┘
┌──────────────────────┐              │
│     FAISS Index      │              │
│                     │              ▼
│ • Flat IP Index     │       ┌──────────────────┐
│ • Normalized Vectors│       │   LLM Model      │
│ • Fast Similarity   │       │   (GPU/CPU)      │
│ • Cosine Distance   │       │                  │
└─────────────────────┘       │ Running locally  │
                              │ on your machine  │
                              └──────────────────┘
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│  USER SUBMITS QUESTION                                      │
│  "What does Krishna teach about duty?"                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
          ┌────────────────────────────┐
          │  Frontend Validation       │
          │  - Check not empty         │
          │  - Show loading spinner    │
          └────────────────┬───────────┘
                           │
                           ▼
          ┌────────────────────────────┐
          │  Send to Backend           │
          │  POST /query               │
          │  {question, top_k, ...}    │
          └────────────────┬───────────┘
                           │
                           ▼ HTTP
          ┌────────────────────────────┐
          │  Backend Receives Request  │
          │  Validates input           │
          └────────────────┬───────────┘
                           │
                ┌──────────┴──────────┐
                │                     │
                ▼                     ▼
        ┌──────────────┐       ┌────────────────┐
        │ RAG Engine   │       │ LLM Client     │
        └──────────┬───┘       └─────┬──────────┘
                   │                 │
        1. Convert question to        │
           embedding vector           │
                   │                 │
        2. Normalize vector           │
                   │                 │
        3. Search FAISS index         │
           for similar passages       │
                   │                 │
        4. Retrieve top-k passages    │
           (typically 3-5)            │
                   │                 │
        5. Build context string       │
           from passages              │
                   │                 │
                   ▼                 │
        ┌──────────────────┐         │
        │ Retrieved:       │         │
        │ • Passage 1 ✓    │         │
        │ • Passage 2 ✓    │         │
        │ • Passage 3 ✓    │         │
        └──────────────────┘         │
                                     │
                   ┌─────────────────┘
                   │
                   ▼ Context Injection
        ┌─────────────────────────┐
        │ Build LLM Prompt:       │
        │                         │
        │ System: "You are a      │
        │  knowledgeable guide    │
        │  to the Bhagavad Gita"  │
        │                         │
        │ Passages:               │
        │ [retrieved context]     │
        │                         │
        │ Question:               │
        │ "What does Krishna..."  │
        │                         │
        │ Answer:                 │
        └────────────┬────────────┘
                     │
                     ▼ Send to LLM
        ┌─────────────────────────┐
        │ LLM Processing          │
        │                         │
        │ • Tokenize prompt       │
        │ • Run inference         │
        │ • Generate tokens       │
        │ • Assemble response     │
        └────────────┬────────────┘
                     │
                     ▼
        ┌─────────────────────────┐
        │ Generated Answer:       │
        │                         │
        │ "According to the       │
        │  Bhagavad Gita,         │
        │  Krishna teaches that   │
        │  duty (dharma) is the   │
        │  righteous action..."   │
        └────────────┬────────────┘
                     │
                     ▼ Format Response
        ┌─────────────────────────┐
        │ Build JSON Response:    │
        │ {                       │
        │  question: "...",       │
        │  answer: "...",         │
        │  retrieved: [...],      │
        │  passage_count: 3       │
        │ }                       │
        └────────────┬────────────┘
                     │
                     ▼ HTTP
        ┌─────────────────────────┐
        │ Send to Frontend        │
        │ (JSON)                  │
        └────────────┬────────────┘
                     │
                     ▼
        ┌─────────────────────────┐
        │ Frontend Receives       │
        │ - Hide loading spinner  │
        │ - Display answer        │
        │ - Show sources          │
        │ - Enable interactions   │
        └─────────────────────────┘
```

---

## Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────┐
│  FRONTEND (index.html + app.js + styles.css)              │
│                                                            │
│  User Input ──────┐                                       │
│                   │                                       │
│                   ▼                                       │
│  ┌─────────────────────────────┐                         │
│  │  Event Listeners (app.js)   │                         │
│  │  - onClick handlers         │                         │
│  │  - Input validation         │                         │
│  │  - Loading state management │                         │
│  └────────────┬────────────────┘                         │
│               │ fetch()                                  │
└───────────────┼──────────────────────────────────────────┘
                │
         HTTP  │  JSON
         POST  │
                │
┌───────────────▼──────────────────────────────────────────┐
│  MAIN.PY (FastAPI Application)                           │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │  @app.post('/query')                              │  │
│  │  async def query(req: QueryRequest)               │  │
│  │                                                   │  │
│  │  ├─ Validate request                             │  │
│  │  ├─ Initialize RAGEngine instance               │  │
│  │  └─ Process query through pipeline               │  │
│  └────────────┬──────────────────────────────────────┘  │
│               │                                          │
├───────────────┼──────────────────────────────────────────┤
│   RAG ENGINE (rag_engine.py + app/rag.py)              │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │  BhagavadGitaRAGEngine                            │  │
│  │                                                   │  │
│  │  load_corpus()                                    │  │
│  │  ├─ Read geetha_verses.txt                       │  │
│  │  └─ Parse into passages                          │  │
│  │                                                   │  │
│  │  build_embeddings_index()                        │  │
│  │  ├─ Generate embeddings (SentenceTransformers)   │  │
│  │  ├─ Normalize vectors                            │  │
│  │  └─ Create FAISS index                           │  │
│  │                                                   │  │
│  │  search_passages(question)                       │  │
│  │  ├─ Embed question                               │  │
│  │  ├─ Search FAISS index                           │  │
│  │  └─ Return top-k passages                        │  │
│  │                                                   │  │
│  │  generate_answer(question, passages)             │  │
│  │  ├─ Build prompt with context                    │  │
│  │  └─ Call LLM client                              │  │
│  └────────────┬───────────────────────────────────┬──┘  │
│               │                                   │      │
│        FAISS  │                             LLM   │      │
│        Index  │                            Client │      │
│               │                                   │      │
└───────────────┼─────────────────────────────────┼───────┘
                │                                   │
       ┌────────▼───────┐                  ┌───────▼─────────┐
       │                │                  │                 │
       │  FAISS Index   │                  │ LOCAL_LLM.PY   │
       │  (Embedding    │                  │                 │
       │   Search)      │                  │ LocalLLMClient  │
       │                │                  │                 │
       │ • IndexFlatIP  │                  │ Methods:        │
       │ • Normalize    │                  │ ┌─────────────┐ │
       │ • Search       │                  │ │ generate()  │ │
       │ • Cosine Sim   │                  │ │ chat()      │ │
       │                │                  │ │ is_avail()  │ │
       │ Result:        │                  │ │ list_models │ │
       │ [idx1, idx2...]│                  │ └─────────────┘ │
       │ Scores: [...]  │                  │                 │
       └────────────────┘                  │ HTTP Client:    │
                │                          │ requests lib    │
                │                          │                 │
                │                          └────────┬────────┘
                │                                   │
                │            ┌──────────────────────┘
                │            │
                │            ▼
                │    ┌──────────────────┐
                │    │ Ollama Service   │
                │    │ (localhost:11434)│
                │    │                  │
                │    │ Available Models:│
                │    │ ├─ mistral       │
                │    │ ├─ llama2        │
                │    │ ├─ neural-chat   │
                │    │ └─ ...more       │
                │    │                  │
                │    │ /api/generate    │
                │    │ /api/chat        │
                │    │ /api/tags        │
                │    └──────────────────┘
                │
                └─ Retrieved Passages + Generated Answer
```

---

## Request/Response Flow

```
REQUEST:
┌─────────────────────────────────────────┐
│ POST /query HTTP/1.1                    │
│ Host: localhost:8000                    │
│ Content-Type: application/json          │
│                                         │
│ {                                       │
│   "question": "What is yoga?",          │
│   "top_k": 3,                           │
│   "temperature": 0.7,                   │
│   "max_tokens": 512                     │
│ }                                       │
└────────────────┬────────────────────────┘
                 │
        PROCESSING PIPELINE:
                 │
        ┌────────▼────────┐
        │ RAG Engine      │
        │ search_passages │
        └────────┬────────┘
                 │
        ┌────────▼─────────────┐
        │ FAISS Search Result  │
        │                      │
        │ [Passage 1]  score:  │
        │ [Passage 2]  score:  │
        │ [Passage 3]  score:  │
        └────────┬─────────────┘
                 │
        ┌────────▼──────────────┐
        │ LLM generate_answer   │
        │                       │
        │ Input: Question +     │
        │        Context        │
        │ Output: Generated     │
        │         Answer        │
        └────────┬──────────────┘
                 │
        ┌────────▼────────────────┐
        │ Format Response         │
        │ (JSON)                  │
        └────────┬─────────────────┘

RESPONSE:
┌──────────────────────────────────────────┐
│ HTTP/1.1 200 OK                          │
│ Content-Type: application/json           │
│                                          │
│ {                                        │
│   "question": "What is yoga?",           │
│   "answer": "According to the Gita, ...",│
│   "retrieved": [                         │
│     "Verse 1 content...",                │
│     "Verse 2 content...",                │
│     "Verse 3 content..."                 │
│   ],                                     │
│   "passage_count": 3                     │
│ }                                        │
└──────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│         DEVELOPMENT ENVIRONMENT                     │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │ Your Machine (Windows/Mac/Linux)             │  │
│  │                                              │  │
│  │  Port 3000 ◄─── Frontend (HTML/CSS/JS)      │  │
│  │  Port 8000 ◄─── Backend (FastAPI)           │  │
│  │  Port 11434 ◄─ Ollama (Local LLM)           │  │
│  │                                              │  │
│  │  ┌────────────────────────────────────────┐ │  │
│  │  │ Data & Models                          │ │  │
│  │  │ • Corpus (geetha_verses.txt)          │ │  │
│  │  │ • FAISS Index                         │ │  │
│  │  │ • Embeddings (SentenceTransformers)  │ │  │
│  │  │ • LLM Model (mistral/llama2/etc)     │ │  │
│  │  └────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘

Resource Requirements:
┌──────────────┬──────────┬──────────┬──────────┐
│ Component    │ Minimum  │ Recommended │ Notes  │
├──────────────┼──────────┼──────────┼──────────┤
│ RAM          │ 4GB      │ 8GB+     │ For LLM │
│ Disk         │ 10GB     │ 20GB+    │ Models  │
│ GPU          │ Optional │ CUDA 11+ │ Speed   │
│ Processor    │ Any      │ i7+      │ Speed   │
│ Network      │ Optional │ Preferred│ Remote  │
└──────────────┴──────────┴──────────┴──────────┘

Production Deployment (Future):
┌─────────────────────────────────────────────────────┐
│ Docker Container                                    │
│                                                     │
│ FROM python:3.10                                    │
│ RUN pip install -r requirements.txt                 │
│ COPY . /app                                         │
│ EXPOSE 8000                                         │
│ CMD ["python", "backend/main.py"]                   │
│                                                     │
└─────────────────────────────────────────────────────┘

Load Balancing (Future):
                 ┌────────────┐
                 │ Load       │
                 │ Balancer   │
                 └────┬───────┘
          ┌──────────┼──────────┐
          │          │          │
    ┌─────▼──┐ ┌────▼────┐ ┌──▼──────┐
    │Backend │ │Backend  │ │ Backend │
    │Instance│ │Instance │ │Instance │
    └────┬──┘ └─────┬────┘ └────┬────┘
         │          │           │
         └──────┬───┴───┬───────┘
              Cache   DB
```

---

## Database Integration (Future)

```
┌──────────────────────────────────┐
│  FastAPI Backend                 │
│  (Enhanced with ORM)             │
└──────────────┬───────────────────┘
               │
        ┌──────▼──────┐
        │  SQLAlchemy │
        │  (ORM)      │
        └──────┬──────┘
               │
        ┌──────▼──────────┐
        │  PostgreSQL      │
        │                  │
        │ Tables:          │
        │ • queries        │
        │ • results        │
        │ • user_history   │
        │ • bookmarks      │
        └──────────────────┘

Caching Layer:
┌──────────────┐
│  Redis       │
│  Cache       │
│              │
│ • Query      │
│   results    │
│ • Embeddings │
│ • Models     │
└──────────────┘
```

---

Ready for implementation! 🚀
